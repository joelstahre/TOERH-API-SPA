<h2>Reminder</h2>

<%= form_tag reminders_path, :method => :post do %>
	<div>
		<%= label_tag :email %>
		<%= text_field_tag :email, params[:email] %>
	</div>
	<div><%= submit_tag "Send my Auth Token", :class => 'btn btn-mini btn-primary' %></div>
<% end %>